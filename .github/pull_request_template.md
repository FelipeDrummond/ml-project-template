## Summary

<!-- 1-3 bullets: what changed, why -->

## Verification

- [ ] `make check` (lint + types + tests) passes
- [ ] `make smoke` passes locally (if training path was touched)
- [ ] `docs/methods.md` and/or `docs/experiments/` updated if behavior changed
- [ ] If a tracked run was performed, MLflow run id is linked above

## Notes

<!-- gotchas, follow-ups, deferred work -->
